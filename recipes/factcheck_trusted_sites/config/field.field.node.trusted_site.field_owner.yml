langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_owner
    - node.type.trusted_site
id: node.trusted_site.field_owner
field_name: field_owner
entity_type: node
bundle: trusted_site
label: 'Owner / parent organization'
description: 'Owning company, foundation or news agency (e.g. "Reuters", "News Corp"). Domains sharing an owner count as ONE independent source when a claim''s evidence is summarized — republished wire copy is not independent confirmation.'
required: false
translatable: false
default_value: {}
default_value_callback: ''
settings: {}
field_type: string
