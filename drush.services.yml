services:
  ai_provider_universal.commands:
    class: Drupal\ai_provider_universal\Commands\UniversalCommands
    arguments:
      - '@entity_type.manager'
      - '@ai.provider'
    tags:
      - { name: drush.command }
